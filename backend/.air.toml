root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/api ./cmd/api"
bin = "tmp/api"
include_ext = ["go"]
exclude_dir = ["tmp"]
exclude_regex = ["_test\\.go"]
delay = 200

[log]
main_only = true
